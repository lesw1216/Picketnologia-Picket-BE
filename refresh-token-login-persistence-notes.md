# RefreshToken Login Persistence Notes

## 질문 배경

고민한 요구사항은 다음과 같음.

- `RefreshToken` 쿠키의 `Path`를 상위로 확장했을 때,
  모든 웹 브라우저가 닫힌 뒤 다시 웹 서비스에 접속하면 `RefreshToken` 존재 여부를 보고 자동 로그인처럼 동작하게 하고 싶음
- 이미 로그인된 상태에서 새 탭을 열어도 로그인 상태가 유지되게 하고 싶음
- 이때 `AccessToken`은 이전 탭과 새 탭이 같은 것을 쓰는지, 아니면 다른 것을 쓰는지 궁금함
- 새 탭에서 재발급이 일어난 뒤 이전 탭으로 돌아왔을 때 로그인 유지가 어떻게 되는지도 궁금함
- 추가로 프론트 최초 진입 시점은 어디서 판단하는지, `router`에서 처리하는 것이 맞는지도 궁금함

## 1. RefreshToken 쿠키 Path는 어떻게 잡는가

### 결론

- 로그인 유지 요구사항이 있다면 `RefreshToken` 쿠키 `Path`는 보통 `/`로 두는 것이 가장 단순함

### 이유

- 앱 최초 진입 시점의 자동 로그인 복구 요청
- `/token/reissue`
- `/logout`
- 기타 인증 상태 복구가 필요한 요청

위 경로들에서 모두 `RefreshToken` 쿠키가 자동 전송되어야 하기 때문

### 참고

- `Path=/token/reissue`처럼 너무 좁게 잡으면 `/logout`이나 앱 최초 진입 시 사용하는 다른 인증 복구 요청에는 쿠키가 안 감
- 인증 관련 엔드포인트를 전부 `/token/**` 하위로 묶을 수 있다면 `Path=/token`도 가능함
- 하지만 서비스 전반에서 자동 로그인 복구를 쉽게 처리하려면 `Path=/`가 가장 실용적임

## 2. 브라우저를 모두 닫았다가 다시 접속했을 때 로그인 유지 흐름

### 전제

- `AccessToken`은 세션 쿠키 또는 짧은 수명의 쿠키
- `RefreshToken`은 영속 쿠키
- `RefreshToken`은 브라우저 종료 후에도 남아 있음

### 시퀀스

1. 사용자가 로그인 성공
2. 서버가 `AccessToken`, `RefreshToken` 발급
3. 브라우저는 `RefreshToken`을 영속 쿠키로 저장
4. 사용자가 브라우저를 모두 종료
5. `AccessToken`이 세션 쿠키라면 사라짐
6. 나중에 사용자가 다시 웹 서비스 접속
7. 프론트가 "현재 인증 상태 복구 필요"라고 판단
8. 프론트가 `/token/reissue` 같은 재발급 API 호출
9. 브라우저가 저장된 `RefreshToken` 쿠키를 자동 전송
10. 서버가 `RefreshToken` 유효성 검사
11. 유효하면 새 `AccessToken` 발급
12. 프론트가 로그인 상태를 복구

### 핵심

- 로그인 유지의 핵심은 `AccessToken`이 살아 있는 것이 아니라 `RefreshToken`이 살아 있는 것
- 즉, 브라우저를 닫은 뒤에도 자동 로그인처럼 동작하게 하려면 `RefreshToken`이 영속 쿠키여야 함

## 3. 로그인된 상태에서 새 탭을 열었을 때 로그인 유지 흐름

### 전제

- 같은 브라우저의 같은 사이트 탭은 쿠키 저장소를 공유함

### 시퀀스

1. 탭 A에서 이미 로그인 상태
2. 탭 B를 새로 열어 같은 웹 서비스 접속
3. 탭 B도 같은 쿠키 저장소를 참조
4. `AccessToken`이 유효하면 바로 로그인 상태로 API 요청 가능
5. 만약 `AccessToken`이 없거나 만료되었으면 `RefreshToken`으로 재발급 시도
6. 재발급 성공 시 탭 B도 로그인 상태가 됨

### 핵심

- 새 탭은 별도의 로그인을 다시 하는 것이 아니라, 같은 브라우저 쿠키를 공유하기 때문에 로그인 유지처럼 보이는 것

## 4. 새 탭과 이전 탭은 같은 AccessToken을 쓰는가

### 일반적인 경우

- 같은 브라우저 쿠키 저장소를 공유하므로 보통 같은 `AccessToken`을 사용함

### 경우 1. 기존 AccessToken이 아직 유효함

- 탭 A와 탭 B가 같은 `AccessToken` 쿠키 값을 사용

### 경우 2. 새 탭에서 재발급이 일어남

1. 탭 B가 `RefreshToken`으로 새 `AccessToken`을 발급받음
2. 브라우저 쿠키 저장소의 `AccessToken` 값이 새 토큰으로 갱신됨
3. 이후 탭 A도 다음 요청부터 새 `AccessToken`을 사용

### 결론

- 탭별로 독립적인 `AccessToken`을 따로 들고 있는 구조가 아니라,
  브라우저 쿠키 저장소의 현재 값을 함께 사용한다고 보면 됨

## 5. 새 탭에서 사용하다가 이전 탭으로 돌아왔을 때 AccessToken은 어떻게 되는가

### 흐름

1. 탭 A 로그인 상태
2. 탭 B 오픈
3. 탭 B에서 `AccessToken` 만료로 재발급 발생
4. 브라우저 쿠키의 `AccessToken` 값이 새 값으로 교체
5. 다시 탭 A로 돌아감
6. 탭 A가 새로운 API 요청을 보낼 때 최신 쿠키 값이 자동으로 사용됨

### 핵심

- 이전 탭이 과거 `AccessToken`을 계속 별도로 유지하는 것이 아니라,
  다음 요청 시 브라우저의 최신 쿠키를 사용함

## 6. 프론트는 최초 진입 시점을 어떻게 아는가

### 결론

- 보통 프론트가 판단함
- 서버가 먼저 "최초 진입"을 아는 것이 아니라, 프론트 앱이 시작될 때 인증 복구를 시도함

### 어디서 처리하는가

다음 두 위치가 일반적임.

1. 앱 시작 시점
2. 보호된 라우트 진입 시점

### 구현 위치 예시

- 최상위 앱 컴포넌트의 초기화 로직
- `router.beforeEach`
- 인증 상태 저장소의 `tryRestoreSession()` 같은 메서드

### 권장 구조

- 라우터는 "언제 체크할지"만 결정
- 실제 `/token/reissue` 호출은 `authStore` 또는 `authService`가 담당

즉, `router.js`에서 직접 모든 인증 로직을 처리하기보다,
인증 복구 호출을 별도 인증 계층으로 분리하는 것이 더 적절함

## 7. 로그인 유지 정책별 쿠키 전략

### 정책 A. 브라우저 종료 시 로그인 종료

- `AccessToken`: 세션 쿠키
- `RefreshToken`: 세션 쿠키 또는 아주 짧은 영속 쿠키
- 브라우저를 닫으면 로그인 종료

### 정책 B. 브라우저 종료 후에도 로그인 유지

- `AccessToken`: 짧은 수명
- `RefreshToken`: 영속 쿠키
- 브라우저를 닫아도 `RefreshToken`은 남아 있음
- 재접속 시 `RefreshToken`으로 `AccessToken` 재발급

## 최종 정리

- 자동 로그인처럼 보이게 하려면 핵심은 `RefreshToken` 유지임
- `RefreshToken` 쿠키 `Path`는 이 요구사항 기준으로 `/`가 가장 단순함
- 브라우저를 닫았다가 다시 열어도 로그인 유지가 되려면 `RefreshToken`은 영속 쿠키여야 함
- 새 탭과 기존 탭은 쿠키 저장소를 공유하므로 같은 인증 상태를 공유함
- 한 탭에서 `AccessToken`이 재발급되면 다른 탭도 이후 요청부터 최신 쿠키 값을 사용함
- 프론트는 앱 시작 시점 또는 보호된 라우트 진입 시점에 세션 복구를 시도하는 방식이 일반적임

